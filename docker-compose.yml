services:
  glassui:
    build: .
    container_name: glassui
    restart: unless-stopped
    ports:
      - "3080:80"
    networks:
      - default
    # Uncomment below if using external nginx proxy network
    # networks:
    #   - nginx-proxy

# Uncomment to connect to external nginx network
# networks:
#   nginx-proxy:
#     external: true
